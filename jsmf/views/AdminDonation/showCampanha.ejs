<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>User Detail</title>
    <link rel='stylesheet' href='/stylesheets/registerStyle.css' />
</head>

<body>
    <div class="container">
        <div class="justify-content-md-center">
            <h1 class="font-weight-bold">Campanha Detail</h1>
            <table class="table table-striped table-bordered table-dark">
                <tbody>
                    <tr>
                        <td>estado</td>
                        <td><%= campanha.estado %></td>
                    </tr>
                    <tr>
                        <td>description</td>
                        <td><%= campanha.description %></td>
                    </tr>
                    <tr>
                    <tr>
                        <td>Target Value</td>
                        <td><%= campanha.targetValue %></td>
                    </tr>
                    <tr>
                        <td>Logo Name</td>
                        <td><%= campanha.logoName %></td>
                    </tr>
                    <tr>
                        <td>IBAN</td>
                        <td><%= campanha.IBAN %></td>
                    </tr>
                    <tr>
                        <td>Responsaveis</td>
                        <td>
                            <table class="table table-striped table-bordered table-dark">

                                <% for(var i =0; i < campanha.responsaveis.length; i++) { %>
                                <tr>
                                    <td><%= campanha.responsaveis[i] %></td>
                                </tr>
                                <% } %>

                            </table>
                        </td>
                    </tr>
                    <tr>
                        <td>Donations</td>
                        <td>
                            <table class="table table-striped table-bordered table-dark">
                                <% if( campanha.donations != null) { %>
                                <% for(var i =0; i < campanha.donations.length; i++) { %>
                                <tr>
                                    <td><%= campanha.donations[i].user %></td>
                                    <td><%= campanha.donations[i].montante %></td>
                                    <td><%= campanha.donations[i].estado %></td>
                                </tr>
                                <% } %>
                                <% } %>
                            </table>
                        </td>

                    </tr>
                </tbody>
            </table>
            <br>
            <h3><a href="/admin/editCamp/<%= campanha._id%>">EDIT Campanha</a></h3>
            <form action="/admin/deleteCamp/<%= campanha._id%>" method="POST">
                <button type="submit" class="btn btn-danger">DELETE</button>
            </form>
        </div>
    </div>
</body>

</html>